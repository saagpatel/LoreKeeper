name: quality-gates

on:
  pull_request:
  push:
    branches: [main, master]

jobs:
  quality:
    name: quality-gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Policy checks
        run: node scripts/ci/require-tests-and-docs.mjs

      - name: Verify commands
        run: |
          if [ "${GITHUB_EVENT_NAME}" = "push" ]; then
            npm run verify:frontend
            npm run perf:bundle
            npm run perf:build
            npm run perf:assets
            npm run perf:memory
          else
            bash .codex/scripts/run_verify_commands.sh
          fi

      - name: Diff coverage
        run: |
          if [ ! -f coverage/lcov.info ]; then
            echo "coverage/lcov.info not present; skipping diff coverage"
            exit 0
          fi

          compare_branch="origin/${GITHUB_BASE_REF:-main}"
          if ! git rev-parse --verify "$compare_branch" >/dev/null 2>&1; then
            compare_branch="origin/main"
          fi

          if ! git rev-parse --verify "$compare_branch" >/dev/null 2>&1; then
            compare_branch="origin/master"
          fi

          python -m pip install --upgrade pip diff-cover
          diff-cover coverage/lcov.info --compare-branch="$compare_branch" --fail-under=90

      - name: Upload test artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: test-artifacts
          path: |
            playwright-report/
            test-results/
            coverage/
